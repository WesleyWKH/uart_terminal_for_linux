UART Terminal V1.1
==================

This folder contains a standalone Linux x86_64 program. Python does not
need to be installed on the target PC.

Run:
  chmod +x uart_terminal_1_1
  ./uart_terminal_1_1

The PC still needs permission to open serial ports (usually the dialout
group). This binary is for Linux x86_64 only.
